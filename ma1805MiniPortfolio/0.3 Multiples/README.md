03 Multiples

Uses an array of words and a counter variable to display a different 
word every frame. Frame rate is slowed down so the text is readable, 
and a conditional resets the counter once it reaches the end of the array.

Challenges
The text was changing too fast at first, so I used frameRate() to slow 
the whole sketch down.