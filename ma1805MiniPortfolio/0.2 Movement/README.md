 02 Movement

An animated version of the drawing sketch. The mouth grows using a 
variable that increases every frame, then resets once it gets too big.

 Challenges
I had to add an if statement to stop the shape growing forever, 
otherwise it just kept getting bigger off screen.